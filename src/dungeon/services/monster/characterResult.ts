export const characterResult = (
  monsterLevel: number,
  dungeonLevel: number
): string => {
  return `(TODO: Roll Character Subtable for monsterLevel ${monsterLevel} and dungeonLevel ${dungeonLevel}.)`;
};
